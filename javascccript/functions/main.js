let X =(Math.floor(Math.random() * 100));
let Y =(Math.floor(Math.random() * 100));
let GreaterNum;

const result = prompt(X + ` or ` + Y);


function CorrectAnswer(){
 let result = false;
 if (GreaterNum <= result) {
    result = true;
 }

 console.log(result)

}